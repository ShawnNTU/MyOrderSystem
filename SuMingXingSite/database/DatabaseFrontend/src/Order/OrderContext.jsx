import { createContext } from "react";

export const CustomerInfoContext = createContext(null);
export const CustomerInfoDispatchContext = createContext(null);

export const ItemContext = createContext(null);
export const SubItemContext = createContext(null);

export const ItemListDispatchContext = createContext(null);

export const CounterContext = createContext(null)
export const SetCounterContext = createContext(null)

// ================= =====================

export const ReloadContext = createContext(null)